spring:
  application:
    name: ecom-project
  datasource:
    url: jdbc:h2:mem:telusko
    driverClassName: org.h2.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
    db:
      connection: "{connectionString: 'http://__', userName: 'user', password: '1234'}"
      host: 127.0.0.1
      port: 1200