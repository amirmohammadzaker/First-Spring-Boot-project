spring:
  application:
    name: ecom-project
  profiles:
    active: prod
  servlet:
    multipart:
      max-file-size: 2MB
      max-request-size: 2MB

logging:
  level:
    org:
      hibernate:
        orm:
          jdbc:
            bind: TRACE

      springframework:
        security=TRACE: