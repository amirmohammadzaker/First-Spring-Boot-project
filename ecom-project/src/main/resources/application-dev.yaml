spring:
  datasource:
    url: jdbc:h2:mem:devdb
    driverClassName: org.h2.Driver
  sql:
    init:
      mode: always
      data-locations: classpath:data-dev.sql
  jpa:
    show-sql: true
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update
    db:
      connection: "{connectionString: 'http://__', userName: 'devdb', password: '1234'}"
      host: 129.0.0.1
      port: 2400

sms:
  enabled: true